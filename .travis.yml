language: ruby
rvm:
 - 2.5.1
cache: bundler
services:
  - postgresql
before_script:
  - ./bin/webpack
  - gem install rubocop
  - bundle exec rake db:create db:schema:load db:migrate
script:
  - rubocop
  - bin/rake
