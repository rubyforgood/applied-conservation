<section class="page-section">
  <header class="page-header">
    <h2 class="heading page-heading"><%= @target.name %></h2>
    <%= link_to('Edit', edit_target_path(@target), class: 'navigation') %>
  </header>
  <section class="health-attributes">
    <h3>Health Attributes</h3>
    <table class="index-table" >
      <thead>
        <tr>
          <th>Name</th>
          <th>Rating</th>
        </tr>
      </thead>
      <tbody>
        <% @target.health_assessments.each do |health_attr_rating| %>
          <tr>
            <th><%= link_to health_attr_rating.name, health_assessment_path(health_attr_rating) %></th>
            <th>
              <%= best_in_place health_attr_rating, :rating, as: :select,
                                place_holder: "click me to edit",
                                class: 'button-secondary',
                                collection: {"good" => "Good", "bad" => "Bad"},
                                url: health_assessment_path(health_attr_rating) %>
            </th>
          </tr>
        <% end %>
      </tbody>
    </table>
  </section>
</section>
